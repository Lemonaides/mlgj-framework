#ifndef HELPER_H
#define HELPER_H

enum GameState { MENU, NEWGAME, CONTINUE, LOADGAME, SAVEGAME, OPTIONS, MAPEDITOR, NPCEDITOR, QUIT };
enum Direction { UP = 0, DOWN, LEFT, RIGHT };

// this is a good place to define global constants


#endif // HELPER_H
